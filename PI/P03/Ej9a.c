#include <stdio.h>

//crear un programa que cuente espacios, tabs y enters

int main(void){
    int c, espacio, tabuladores, enters;
    while ((c=getchar()) != EOF){
        switch (c) {
            case 32: espacio += 1;
            case 9: tabuladores += 1;
            case
        }
    }
}